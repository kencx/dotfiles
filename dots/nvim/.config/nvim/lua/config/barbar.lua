--[[vim.g.bufferline = {

    animation = true,
    auto_hide = false,  -- disable auto-hide when single tab
    tabpages = true,    -- enable tabpages indicator (top right)
    closable = true,    -- enable close button
    clickable = true,
    icons = true,
    icon_custom_colors = false,

    insert_at_end = false,
    insert_at_start = false,

    maximum_padding = 1,
    maximum_length = 30,

    no_name_title = nil,
}
]]
