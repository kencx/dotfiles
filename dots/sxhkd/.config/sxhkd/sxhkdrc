
# floating terminal
super + Return
	kitty

# tiled terminal
super + v
    kitty --class tiled

# neovim
super + d
    kitty --class dev nvim

# rofi
super + space
	rofi -show drun

# keybindings
super + @slash
	$HOME/bin/keybindings | rofi -dmenu -i -p "hotkeys"

# configs
super + shift + z
    kitty --class configs $HOME/bin/configs

# toggle bluetooth
super + b
    $HOME/bin/rofi-bluetooth

# wifi menu
super + shift + c
    $HOME/bin/nmcli-rofi

# toggle scratchpad
super + u
    $HOME/bin/scratchpad scratchpad

# todo tasks
alt + t
       todofi.sh

# reload sxhkd
super + Escape
	pkill -USR1 -x sxhkd

#
# bspwm hotkeys
#

# quit/restart bspwm
super + alt + {q,r}
	bspc {quit,wm -r}

# close and kill
super + {_,shift + }w
	bspc node -{c,k}

#
# state/flags
#

# set the window state
super + {t,shift + t,s,f}
	bspc node -t {tiled,pseudo_tiled,floating,fullscreen}

# super + n turns on window mode

# toggle locked, sticky, private node flag
super + n : super + {l,s,p}
	bspc node -g {locked,sticky,private} || \
    bspc node -g {locked=off,sticky=off,private=off}

# {hide, unhide} current window
super + n : super + h
    bspc node focused -g hidden=on

super + n : super + shift + h
    bspc node $(bspc query -N -d focused -n .hidden) -g hidden=off

# {hide, unhide} all windows

# toggle all focused windows between floating and tiled state
super + n : super + f
    bspc query -N -d focused | xargs -I node_id bspc node node_id -t {tiled,pseudo_tiled}

#
# focus/swap
#

# swap the current node and the biggest node
super + g
	bspc node -s biggest.local

# {focus, swap} the node in the given direction
super + {_,shift + }{h,j,k,l}
	bspc node -{f,s} {west,south,north,east}

# focus {next, prev} node
super +
    bspc node -{f,s} {next,prev}.local.!hidden.window

# rotate nodes {clockwise, anti-clockwise}
super + shift + {d,a}
    bspc node @/ -C {forward,backward}

# flip nodes vertically, horizontally
super + n : super + {_,shift + } f
    bspc node @/ -F {vertical, horizontal}

# move window to chosen preselection
super + shift + m
    bspc node -n last.!automatic

# focus the next/previous desktop in the current monitor
super + bracket{left,right}
	bspc desktop -f {prev,next}.local

# focus the last node/desktop
super + {grave,Tab}
	bspc {node,desktop} -f last

# {focus, send node to} {1-5} desktop
super + n : {_, super + } KP_{End,Down,Next,Left,Begin}
	bspc {desktop -f, node -d} '^{1-5}' --follow

# send all windows in current desktop to chosen desktop

#
# preselect
#

# preselect the direction
super + ctrl + {h,j,k,l}
	bspc node -p {west,south,north,east}

# cancel the preselection for the focused node
super + ctrl + space
	bspc node -p cancel


#
# move/resize
#

# super + r turns on resize mode. Esc to exit resize mode

# resize: expand window
super + r : {h,j,k,l}
	bspc node -z {left -20 0,bottom 0 20,top 0 -20,right 20 0}

# resize: contract window
super + r : shift + {h,j,k,l}
	bspc node -z {right -20 0,top 0 20,bottom 0 -20,left 20 0}

# move floating window
super + r : {Left,Down,Up,Right}
	bspc node -v {-20 0,0 20,0 -20,20 0}

# equalize size
super + e
    bspc node @/ --equalize
