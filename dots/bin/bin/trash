#!/bin/bash

trash=$HOME/tmp

if [ ! -d "$trash" ]; then
	mkdir -p "$trash"
fi

clean() {
	/bin/rm -rf $trash/*
}

case "$1" in
	"clear") clean && exit ;;
	*) ;;

esac

mv --force "$@" "$trash"
nohup find "$trash" -type f -atime +7 -exec /bin/rm '{}' \; >/dev/null 2>&1 &
